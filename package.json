{
  "name": "descomprimir-token",
  "version": "1.0.0",
  "description": "descomprime JWT  comprimido con DEFLATE",
  "main": "leerTokenComprimido.js",
  "scripts": {
    "start": "node leerTokenComprimido.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Rafael Ortiz",
  "license": "ISC",
  "dependencies": {
    "zlib": "^1.0.5"
  }
}
